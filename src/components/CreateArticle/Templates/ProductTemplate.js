import React from 'react';

const ProductTemplate = ({ contentType, article, onChangeData }) => {
  return (
    <div>
      <p>Template: Product</p>
    </div>
  );
};

export default ProductTemplate;
